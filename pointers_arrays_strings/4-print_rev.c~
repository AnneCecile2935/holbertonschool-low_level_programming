#include "main.h"
/**
*_rev - print a sentence in reverse
*@str : string parameters
*/
void print_rev(char *str)
{
int len;
len = _strlen(str);
while (len !=0)
{_putchar(*str);
len--;
}
_putchar('\n');
}