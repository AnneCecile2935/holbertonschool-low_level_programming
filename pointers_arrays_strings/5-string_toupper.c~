#include "main.h"
/**
*_string_toupper - compare numbers of characters of two strings
*@*: pointer
*/
char *string_toupper(char *str)
{
char *ptr = str;
while (*ptr)
{
if (*ptr >= 'a' && *ptr <= 'z')
{
*ptr -= ('a' - 'A');
}
ptr++;
}
return (str);
}
