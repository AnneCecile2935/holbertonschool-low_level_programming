#include "main.h"
/**
* mul - function that multiplicate two integers.
* @a: first parameter for function.
* @b: second parameter for function.
* Return: the result
*/
int mul(int a, int b)
{
return (a * b);
}
