#include "main.h"
/**
* _isupper - function that checkx for uppercase character.
* @c: parameter for function.
* Return: 0
*/
int _isupper(int c)
{
if ('A' <= c && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
