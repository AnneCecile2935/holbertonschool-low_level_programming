#include "main.h"
/**
 * main - check the code.
 * _isupper(int c) - function that checkx for uppercase character.
 * Return: 1 if it's an uppercase, 0 otherwise
 */
int _isupper(int c)
{
if ('A' <= c && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
