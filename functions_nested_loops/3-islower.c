#include "main.h"
/**
* _islower - check if is lowercase.
* @ 'c' - caractere.
* Return: 1 if it's a lowercase - 0 if it's not a lowercase.
**/
int _islower(int c)
{
if (c >= 'a' &&  c <= 'z')
{return (1);
}
else
{
return (0);
}
}
