#include <stdio.h>
#include <stdlib.h>
#include <limits.h>


void *malloc_checked(unsigned int b)
{
    void *ptr;
    ptr = malloc(b);
    if (ptr == NULL)
    {
        return NULL;
    }
    return ptr;
}
